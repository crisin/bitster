# Dev only: expo start serves the app from Metro (no relay), so point the
# client at a locally running relay: `node server.js 8090`.
# In production the relay shares the app's origin and this var is not needed.
EXPO_PUBLIC_RELAY_URL=ws://localhost:8090/ws
